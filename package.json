{
	"name": "movie-maker",
	"version": "1.0.0",
	"description": "movie maker",
	"main": "index.js",
	"scripts": {
		"page": "live-server --open=./index.html --port=8081",
		"build": "babel . --out-dir compiled --presets=env,react --ignore=node_modules,compiled --source-maps inline -w"
	},
	"author": "David Hong",
	"license": "UNLICENSED",
	"dependencies": {
		"react": "^0.14.7",
		"react-dom": "^0.14.7"
	},
	"devDependencies": {
		"babel-cli": "^6.6.5",
		"babel-preset-env": "^1.6.1",
		"babel-preset-react": "^6.5.0"
	}
}
